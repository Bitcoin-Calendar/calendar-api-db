# This is an example .env file for the Bitcoin Historical Events API.
# Copy this file to .env in the same directory (api_and_database/.env)
# and replace the placeholder values with your actual configuration.
# Ensure .env is added to your .gitignore file.

# --- Required ---
# Define the API key that clients must use to authenticate.
# Generate a strong, random string for this value.
API_KEY="replace_with_your_actual_api_key"

# --- Optional (Defaults are set in main.go) ---
# PORT="3000"
# DB_PATH="/app/data/events.db" # Path inside the Docker container, usually corresponds to ./data/events.db on host